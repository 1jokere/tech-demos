# tech-demos

Sticky playground for weekday X-bookmark tech demos.

One tiny app per folder under `apps/<slug>/`. Bun. Cloud agents (Fable 5) only touch `apps/`, plan with `skills/project-planning/`, and every demo PR must include a screenshot **and** a video of the running app.

```
AGENTS.md
skills/project-planning/
apps/<slug>/
tracking/seen-bookmarks.json
```
